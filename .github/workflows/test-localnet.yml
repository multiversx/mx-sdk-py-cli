name: Test localnet

on:
  pull_request:
    branches: [main, feat/*]
  workflow_dispatch:

env:
 BRANCH_NAME: ${{ github.head_ref || github.ref_name }}

jobs:
  install:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.11]

    steps:
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip3 install -r requirements.txt

      - name: Smoke test
        run: |
          mkdir ~/multiversx-sdk
          export PYTHONPATH=.
          python3 -m multiversx_sdk_cli.cli testnet prerequisites --configfile=./multiversx_sdk_cli/tests/testdata/testnets/localnet.toml
          python3 -m multiversx_sdk_cli.cli testnet build --configfile=./multiversx_sdk_cli/tests/testdata/testnets/localnet.toml
          python3 -m multiversx_sdk_cli.cli testnet config --configfile=./multiversx_sdk_cli/tests/testdata/testnets/localnet.toml
          
          # Leave the testnet to run for some time, then assert for "time out error" (124).
          timeout 1m python3 -m multiversx_sdk_cli.cli testnet start --configfile=./multiversx_sdk_cli/tests/testdata/testnets/localnet.toml || \
          test $? -eq 124 || \
          echo "Timeout error expected, but something else happened."
